name: ekutir_agent_app
description: eK Acre Growth Flutter mock app for field agents.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  go_router: ^17.1.0
  provider: ^6.1.2
  url_launcher: ^6.3.2
  share_plus: ^12.0.1
  pdf: ^3.12.0
  printing: ^5.14.3
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/reference/
